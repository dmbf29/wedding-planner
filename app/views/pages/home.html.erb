<div class="container text-center mb-5">
  <div class="row justify-content-center">
    <div class="col-12 col-msm-6 col-md-3">
      <%= simple_form_for @upload do |f| %>
        <%= f.input :photos, as: :file, label: "Upload Photos", input_html: { multiple: true, onchange: "this.form.requestSubmit();this.label.textContent = 'Uploading...'" } %>
      <% end %>
    </div>
  </div>
</div>
